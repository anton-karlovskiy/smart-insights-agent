# Secrets
.env
.env.*
!.env.example

# Pipeline output. The committed sample lives in examples/sample_insights.json
# (see SPEC.md §7) so reviewers without an API key can run `evaluate` against it.
out/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
